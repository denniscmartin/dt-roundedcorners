import XCTest
@testable import DTRoundedCorners

final class DTRoundedCornersTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DTRoundedCorners().text, "Hello, World!")
    }
}
